# Firebase Configuration Directory

Please place your Firebase Service Account JSON credentials file here, named:

`firebase-service-account.json`

The backend service resolves credentials from this path to initialize the Firebase Admin SDK.
